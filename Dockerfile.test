# Dockerfile for OSS Migration Test Environment
# Issue #93: pytest実行環境
# 作成日: 2025年7月19日

FROM python:3.12-slim

# 作業ディレクトリ設定
WORKDIR /app

# システム依存関係インストール
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    git \
    curl \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Python依存関係インストール
COPY requirements.txt requirements-oss.txt ./
RUN pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir -r requirements-oss.txt

# テスト用設定ファイルコピー
COPY pytest.ini pytest-oss.ini ./

# アプリケーションコードをマウント（docker-compose.ymlで設定）

# デフォルトコマンド
CMD ["pytest", "-c", "pytest-oss.ini"]
