# 🏗️ フロントエンド開発用Docker（根本解決）
FROM node:18-alpine

WORKDIR /app

# package.jsonのみコピー（依存関係キャッシュ最適化）
COPY package*.json ./

# 依存関係インストール
RUN npm ci --only=production

# ソースコードコピー
COPY . .

# 開発サーバー起動
EXPOSE 3000
CMD ["npm", "run", "dev"]

# === 🎯 根本解決のポイント ===
# 1. node_modules は Docker内のみ存在
# 2. ホストマシンには一切の成果物を残さない
# 3. git管理対象はソースコードのみ
